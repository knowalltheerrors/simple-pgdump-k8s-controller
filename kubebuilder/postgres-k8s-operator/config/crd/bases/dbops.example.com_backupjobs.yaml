---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: backupjobs.dbops.example.com
spec:
  group: dbops.example.com
  names:
    kind: BackupJob
    listKind: BackupJobList
    plural: backupjobs
    shortNames:
    - pgbk
    singular: backupjob
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              image:
                description: Backup runner image (contains pg_dump + Go backup binary)
                type: string
              postgres:
                properties:
                  database:
                    type: string
                  host:
                    type: string
                  passwordSecretRef:
                    properties:
                      key:
                        type: string
                      name:
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  port:
                    format: int32
                    type: integer
                  sslMode:
                    type: string
                  username:
                    type: string
                required:
                - database
                - host
                - passwordSecretRef
                - username
                type: object
              serviceAccountName:
                description: Optional SA for the k8s Job pod
                type: string
              storage:
                properties:
                  accessKeySecretRef:
                    properties:
                      key:
                        type: string
                      name:
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  bucket:
                    type: string
                  endpoint:
                    description: |-
                      AWS S3: keep Endpoint empty (AWS default endpoint resolution)
                      MinIO: set Endpoint, e.g. https://minio.minio.svc:9000
                    type: string
                  forcePathStyle:
                    type: boolean
                  prefix:
                    type: string
                  provider:
                    description: s3 or minio (both go through S3 API client)
                    type: string
                  region:
                    type: string
                  secretKeySecretRef:
                    properties:
                      key:
                        type: string
                      name:
                        type: string
                    required:
                    - key
                    - name
                    type: object
                required:
                - accessKeySecretRef
                - bucket
                - provider
                - secretKeySecretRef
                type: object
              ttlSecondsAfterFinished:
                description: If empty, operator defaults it
                format: int32
                type: integer
            required:
            - postgres
            - storage
            type: object
          status:
            properties:
              finishedAt:
                format: date-time
                type: string
              k8sJobName:
                type: string
              lastRunTime:
                format: date-time
                type: string
              message:
                type: string
              objectKey:
                type: string
              phase:
                type: string
              startedAt:
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
